APP_ENV=development
LOG_LEVEL=INFO

# Local (non-Docker) Postgres. Matches docker-compose.yml's `db` service credentials.
DATABASE_URL=postgresql+psycopg2://mandate_guard:mandate_guard@localhost:5432/mandate_guard
