[pytest]
pythonpath = .
testpaths = tests
markers =
    smoke: network-dependent test against the real Anthropic API (requires ANTHROPIC_API_KEY). Excluded from the default run; include explicitly with `pytest -m smoke`.
addopts = -m "not smoke"
