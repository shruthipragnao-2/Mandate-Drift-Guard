# Test-only fixtures

Fixtures for `backend/tests/` (e.g. sample request payloads for API tests). Distinct from the
product evaluation dataset under `/fixtures` at the repo root, which holds labeled
legitimate/drift/ambiguous cases for the evaluation harness (milestone M5).

Empty in Checkpoint C5.
