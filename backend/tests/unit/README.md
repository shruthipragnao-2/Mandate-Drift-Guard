# Unit tests

Pure, framework-agnostic tests for domain logic — the evidence-engine signal functions, the
policy-gate decision table, and schema validation (evidence packet / LLM output). No DB, no
network.

Empty in Checkpoint C5 (no domain logic exists yet). Populated starting Checkpoint C6, milestones
M1–M2 per `docs/IMPLEMENTATION-PLAN.md` §Q/§M.
